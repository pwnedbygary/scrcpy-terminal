<?xml version="1.0" encoding="utf-8"?>
<!-- The bar sits below the video, never over it: a target's bottom edge holds
     its navigation gestures and the space bar of its on-screen keyboard. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <io.github.pwnedbygary.scterm.viewer.AspectFrameLayout
            android:id="@+id/video_frame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center">

            <SurfaceView
                android:id="@+id/surface"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@string/app_name" />
        </io.github.pwnedbygary.scterm.viewer.AspectFrameLayout>

        <io.github.pwnedbygary.scterm.viewer.RemoteKeyboardView
            android:id="@+id/keyboard_sink"
            android:layout_width="1dp"
            android:layout_height="1dp" />

        <TextView
            android:id="@+id/stats"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_margin="8dp"
            android:background="#99000000"
            android:padding="6dp"
            android:textColor="#FFE0E0E0"
            android:textSize="11sp" />

        <LinearLayout
            android:id="@+id/overlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="#E6101418"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="24dp">

            <ProgressBar
                android:id="@+id/progress"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <TextView
                android:id="@+id/message"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:gravity="center"
                android:maxWidth="320dp"
                android:textColor="#FFFFFFFF" />

            <Button
                android:id="@+id/overlay_close"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="@string/done"
                android:visibility="gone" />
        </LinearLayout>
    </FrameLayout>

    <!-- fillViewport + the row's own gravity centers a short bar; layout_gravity
         would clip the start of a bar wider than the screen. -->
    <HorizontalScrollView
        android:id="@+id/bar_scroll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FF101418"
        android:fillViewport="true"
        android:scrollbars="none">

        <LinearLayout
            android:id="@+id/bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:orientation="horizontal"
            android:padding="4dp" />
    </HorizontalScrollView>
</LinearLayout>
